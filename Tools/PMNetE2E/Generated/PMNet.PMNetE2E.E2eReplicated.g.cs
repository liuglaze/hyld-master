// <auto-generated>
//     本文件由 Tools/PMNetGen 生成，请勿手动修改。
//     网络对象 partial 产物
//     类：PMNetE2E.E2eReplicated
//     稳定键：CLASS:PMNetE2E.E2eReplicated
//     类型 ID：2761782480（0xA49D74D0）
//     类协议摘要：0x50AF2E09
//     复制属性：4 个 / RPC：6 条
// 
//     生成契约：Docs/plans/net-r2-codegen-contract.md（§2 稳定 ID / §4 API 面）
//     语言面：C# 7.3 + .NET Standard 2.0（Unity 2019.4 约束，D-R0-48）
//     重新生成：dotnet Tools/PMNetGen/bin/Release/net8.0/PMNetGen.dll \
//                 --decl-gen <源目录> --out-dir <输出目录> --id-lock Docs/plans/pmnet-ids.json
//     同步校验：build.bat 以 --decl-check 模式比对，不一致即失败
// </auto-generated>

using PMNet;
using System;

namespace PMNetE2E
{
    /// <summary>[PMNetworkObject] 的生成产物（与业务 partial 合成同一个类）。</summary>
    public partial class E2eReplicated
    {
        /// <summary>类型 ID（32 位），与 PMNetRegistry 里的 ClassId 同源（D-R0-49）。</summary>
        public const uint PMGeneratedClassId = 2761782480u;

        /// <summary>对象变更掩码总位数（= 本类复制属性数）。</summary>
        public const ushort PMGeneratedChangeMaskBitCount = 4;

        /// <summary>
        /// 本类复制属性序号在**整个继承链**上的基址（= 已声明祖先的复制属性总数）。
        /// PMRepList 的 index 与 MarkPropertyDirty 的 index 都是每对象连续的，
        /// 派生类若从 0 开始编号会与基类同号条目撞车。
        /// </summary>
        public const int PMGeneratedPropertyIndexBase = 0;

        /// <summary>复制数组的长度上限（防御越界分配；契约 §8 列为待收敛参数，R2 取 4096）。</summary>
        private const int PMGeneratedMaxArrayLength = 4096;

        // ---------------- 编织版本门（冻结格式 v1；见 Docs/plans/net-rpc-weaving-contract.md）----------------

        /// <summary>本类的 RPC 编织版本：0 = 编译后尚未编织；PMNetWeaver 成功编织后改为 1。</summary>
        internal static int PMNet_GetRpcWeaveVersion()
        {
            return 0;
        }

        /// <summary>
        /// 未编织程序集的守卫：版本不为 1 就抛明确异常（附上修复命令方向），否则返回 1。
        /// PMNet_BuildEntry 的开头与实例字段初始化都会调用它。
        /// </summary>
        private static int PMNet_RequireRpcWeave()
        {
            if (PMNet_GetRpcWeaveVersion() != 1)
            {
                throw new System.InvalidOperationException(
                    "PMNet RPC 未编织：本程序集仍处于编译后未处理状态。请先运行 `dotnet PMNetWeaver.dll --weave <assembly.dll>`（构建脚本应在复制 DLL 后执行）。");
            }

            return 1;
        }

        /// <summary>
        /// 实例 guard：字段初始化就调用 Require ⇒ 未编织时 new 直接被拒。
        /// 字段本身不需要被读取，pragma 压掉「已赋值但未使用」的 CS0414。
        /// </summary>
        #pragma warning disable 0414
        private readonly int PMNet_rpcWeaveGate = PMNet_RequireRpcWeave();
        #pragma warning restore 0414

        // ---------------- 复制属性注册 ----------------

        /// <summary>注册本类的复制属性与条件（对应 UE 的 DOREPLIFETIME_WITH_PARAMS_FAST 位置）。</summary>
        protected override void CollectLifetimeReplicatedProps(PMNet.PMRepList outProps)
        {
            base.CollectLifetimeReplicatedProps(outProps);
            outProps.Add(0, PMNet.PMCond.None, true); // _title（PropertyId=15488）
            outProps.Add(1, PMNet.PMCond.None, true); // _speed（PropertyId=26362）
            outProps.Add(2, PMNet.PMCond.OwnerOnly, true); // _ammo（PropertyId=36071）
            outProps.Add(3, PMNet.PMCond.None, true); // _health（PropertyId=43547）
        }

        // ---------------- 赋值即标脏的访问器 ----------------

        /// <summary>
        /// 赋值并标脏（Push 模型，契约 D-R0-13）。
        /// 业务必须在**真实赋值处**改调本访问器：漏标会让该属性静默不同步。
        /// </summary>
        public void PMNet_Set_title(string value)
        {
            _title = value;
            MarkPropertyDirty(0);
        }

        /// <summary>
        /// 赋值并标脏（Push 模型，契约 D-R0-13）。
        /// 业务必须在**真实赋值处**改调本访问器：漏标会让该属性静默不同步。
        /// </summary>
        public void PMNet_Set_speed(float value)
        {
            _speed = value;
            MarkPropertyDirty(1);
        }

        /// <summary>
        /// 赋值并标脏（Push 模型，契约 D-R0-13）。
        /// 业务必须在**真实赋值处**改调本访问器：漏标会让该属性静默不同步。
        /// </summary>
        public void PMNet_Set_ammo(int value)
        {
            _ammo = value;
            MarkPropertyDirty(2);
        }

        /// <summary>
        /// 赋值并标脏（Push 模型，契约 D-R0-13）。
        /// 业务必须在**真实赋值处**改调本访问器：漏标会让该属性静默不同步。
        /// </summary>
        public void PMNet_Set_health(int value)
        {
            _health = value;
            MarkPropertyDirty(3);
        }

        // ---------------- 属性读写器（供 PMPropertyDescriptor 持委托）----------------

        /// <summary>写出 _title 的当前值。</summary>
        private static void PMNet_Write__title(PMNet.PMNetObject t, PMNet.PMNetWriter w)
        {
            E2eReplicated self = (E2eReplicated)t;
            PMNet.PMNetString.WriteBounded(w, self._title);
        }

        /// <summary>读入并赋值 _title。注意：这是一个接收侧写入口，位于本 partial 内所以能访问私有成员。</summary>
        private static void PMNet_Read__title(PMNet.PMNetObject t, PMNet.PMNetReader r)
        {
            E2eReplicated self = (E2eReplicated)t;
            self._title = PMNet.PMNetString.ReadBounded(r);
        }

        /// <summary>写出 _speed 的当前值。</summary>
        private static void PMNet_Write__speed(PMNet.PMNetObject t, PMNet.PMNetWriter w)
        {
            E2eReplicated self = (E2eReplicated)t;
            w.WriteFloat(self._speed);
        }

        /// <summary>读入并赋值 _speed。注意：这是一个接收侧写入口，位于本 partial 内所以能访问私有成员。</summary>
        private static void PMNet_Read__speed(PMNet.PMNetObject t, PMNet.PMNetReader r)
        {
            E2eReplicated self = (E2eReplicated)t;
            self._speed = r.ReadFloat();
        }

        /// <summary>写出 _ammo 的当前值。</summary>
        private static void PMNet_Write__ammo(PMNet.PMNetObject t, PMNet.PMNetWriter w)
        {
            E2eReplicated self = (E2eReplicated)t;
            w.WriteInt32(self._ammo);
        }

        /// <summary>读入并赋值 _ammo。注意：这是一个接收侧写入口，位于本 partial 内所以能访问私有成员。</summary>
        private static void PMNet_Read__ammo(PMNet.PMNetObject t, PMNet.PMNetReader r)
        {
            E2eReplicated self = (E2eReplicated)t;
            self._ammo = r.ReadInt32();
        }

        /// <summary>写出 _health 的当前值。</summary>
        private static void PMNet_Write__health(PMNet.PMNetObject t, PMNet.PMNetWriter w)
        {
            E2eReplicated self = (E2eReplicated)t;
            w.WriteInt32(self._health);
        }

        /// <summary>读入并赋值 _health。注意：这是一个接收侧写入口，位于本 partial 内所以能访问私有成员。</summary>
        private static void PMNet_Read__health(PMNet.PMNetObject t, PMNet.PMNetReader r)
        {
            E2eReplicated self = (E2eReplicated)t;
            self._health = r.ReadInt32();
        }

        // ---------------- RepNotify 分发（0 = 无）----------------

        /// <summary>
        /// 按 PMPropertyDescriptor.OnRepMethodId 分发到业务的表现回调。
        /// 槽位按 PropertyId 升序、只对「有 OnRep」的属性从 1 起算
        /// （与生成期算出的 OnRepMethodId 同源）。
        /// </summary>
        private static void PMNet_OnRepDispatch(PMNet.PMNetObject t, ushort onRepMethodId)
        {
            E2eReplicated self = (E2eReplicated)t;

            switch (onRepMethodId)
            {
                case 1:
                    self.OnRep_Health();
                    return;
                default:
                    return;
            }
        }

        // ---------------- RPC ----------------

        // 实参传递：调用桩用**闭包捕获**本次调用的实参（契约 §4.3）。
        // 刻意不使用「对象上的实参帧」：发送可能被推迟（未接线 / 排队），而编码发生在
        // 推迟之后 —— 届时实参帧可能已被后续同 RPC 调用覆写，于是发出**错误的参数**。

        // 但闭包捕获本身只对**值类型与不可变引用**（string）成立：
        // 数组是可变引用类型，只捕获引用的话，编码时读到的是那个数组「当下」的元素，
        // 而不是调用时的值。尤其 Multicast：服务端会先本地执行、再外发，
        // 业务实现可能就地改写数组参数 —— 远端必须看到**调用时**的值。
        // 因此数组参数在调用点（且在本地执行之前）做一次浅拷贝快照；
        // string 不可变，不需要重复拷贝；长度门则在**入队前**执行。

        /// <summary>RPC Say 的稳定 ID（16 位，D-R0-49）。</summary>
        public const ushort PMGeneratedRpcId_Say = 431;

        /// <summary>读出参数、过校验、调用业务实现（接收侧分发）。</summary>
        private static void PMNet_RpcInvoke_Say(PMNet.PMNetObject t, PMNet.PMNetReader r)
        {
            E2eReplicated self = (E2eReplicated)t;
            string p0 = PMNet.PMNetString.ReadBounded(r);

            if (!r.IsAtEnd)
            {
                throw new System.FormatException("RPC Say 载荷存在尾随字节：参数只占 " + r.Consumed + " 字节，载荷更长");
            }
            self.Say(p0);
        }

        /// <summary>
        /// 发送 helper（契约 §2 冻结格式 v1：**private**，只允许编织后的同类入口 Say 调用）。
        /// 先走 GetFunctionCallspace 判定，再按结果本地执行 / 发往远端。
        /// 业务请调用普通名 Say（编织后它是网络入口），不要直接调用本方法。
        /// </summary>
        private void PMNet_Say(string p0)
        {
            PMNet.PMFunctionCallspace callspace = PMNet.PMRpcDispatch.EvaluateCallspace(
                NetMode, Role, PMNet.PMRpcKind.Client, GetNetConnection() != null, false);

            // 实参预处理（快照 / 长度门）必须在**本地执行之前**：
            // Multicast 在服务端会先本地执行、再外发，业务实现可以就地改写数组实参。
            if (PMNet.PMRpcDispatch.ShouldSendRemote(callspace))
            {
                PMNet.PMNetString.EnsureWithinLimit(p0, "RPC Say 的字符串实参 p0");
            }

            if (PMNet.PMRpcDispatch.ShouldExecuteLocal(callspace))
            {
                Say(p0);
            }

            if (PMNet.PMRpcDispatch.ShouldSendRemote(callspace))
            {
                // 闭包捕获实参；数组用快照（值类型/string 不必）：编码推迟到真正发送时也不会读到被覆盖的值。
                PMNet.Generated.PMNetGeneratedRegistry.EnqueueRemote(
                    this, PMGeneratedRpcId_Say,
                    delegate(PMNet.PMNetObject t, PMNet.PMNetWriter w)
                    {
                        PMNet.PMNetString.WriteBounded(w, p0);
                    });
            }
        }

        /// <summary>RPC Checked 的稳定 ID（16 位，D-R0-49）。</summary>
        public const ushort PMGeneratedRpcId_Checked = 821;

        /// <summary>读出参数、过校验、调用业务实现（接收侧分发）。</summary>
        private static void PMNet_RpcInvoke_Checked(PMNet.PMNetObject t, PMNet.PMNetReader r)
        {
            E2eReplicated self = (E2eReplicated)t;
            int p0 = r.ReadInt32();

            if (!r.IsAtEnd)
            {
                throw new System.FormatException("RPC Checked 载荷存在尾随字节：参数只占 " + r.Consumed + " 字节，载荷更长");
            }

            // 校验（UE 原生 Validate）：返回 false ⇒ 请求断开连接。
            if (!self.Checked_Validate(p0))
            {
                PMNet.PMRpcValidationSink.NotifyValidateFailed(
                    t, PMGeneratedRpcId_Checked, "Checked");

                // 校验发生在生成物内部，它拿不到连接；把结论抛给持有连接的那一层：
                // PMNetRpcReceive.Deliver 会据此向来源连接发出断连请求（D-R0-45）。
                throw new PMNet.PMNetRpcValidationFailedException(
                    PMGeneratedRpcId_Checked, "Checked");
            }
            self.Checked(p0);
        }

        /// <summary>
        /// 发送 helper（契约 §2 冻结格式 v1：**private**，只允许编织后的同类入口 Checked 调用）。
        /// 先走 GetFunctionCallspace 判定，再按结果本地执行 / 发往远端。
        /// 业务请调用普通名 Checked（编织后它是网络入口），不要直接调用本方法。
        /// </summary>
        private void PMNet_Checked(int p0)
        {
            PMNet.PMFunctionCallspace callspace = PMNet.PMRpcDispatch.EvaluateCallspace(
                NetMode, Role, PMNet.PMRpcKind.Server, GetNetConnection() != null, false);

            // 实参预处理（快照 / 长度门）必须在**本地执行之前**：
            // Multicast 在服务端会先本地执行、再外发，业务实现可以就地改写数组实参。
            if (PMNet.PMRpcDispatch.ShouldSendRemote(callspace))
            {
            }

            if (PMNet.PMRpcDispatch.ShouldExecuteLocal(callspace))
            {
                Checked(p0);
            }

            if (PMNet.PMRpcDispatch.ShouldSendRemote(callspace))
            {
                // 闭包捕获实参；数组用快照（值类型/string 不必）：编码推迟到真正发送时也不会读到被覆盖的值。
                PMNet.Generated.PMNetGeneratedRegistry.EnqueueRemote(
                    this, PMGeneratedRpcId_Checked,
                    delegate(PMNet.PMNetObject t, PMNet.PMNetWriter w)
                    {
                        w.WriteInt32(p0);
                    });
            }
        }

        /// <summary>RPC Push 的稳定 ID（16 位，D-R0-49）。</summary>
        public const ushort PMGeneratedRpcId_Push = 6043;

        /// <summary>读出参数、过校验、调用业务实现（接收侧分发）。</summary>
        private static void PMNet_RpcInvoke_Push(PMNet.PMNetObject t, PMNet.PMNetReader r)
        {
            E2eReplicated self = (E2eReplicated)t;
            float[] p0;
            int n = r.ReadSInt32();
            if (n < 0)
            {
                p0 = null;
            }
            else
            {
                if (n > PMGeneratedMaxArrayLength)
                {
                    // 越界长度 = 数据损坏，抛异常而不是静默截断（静默截断会掩盖协议错误）。
                    throw new System.FormatException("PMNet 复制数组长度越界：float[]");
                }

                float[] a = new float[n];
                for (int i = 0; i < n; i++)
                {
                    a[i] = r.ReadFloat();
                }

                p0 = a;
            }

            if (!r.IsAtEnd)
            {
                throw new System.FormatException("RPC Push 载荷存在尾随字节：参数只占 " + r.Consumed + " 字节，载荷更长");
            }

            // 校验（ForceValidate 三态）：Reject 只跳过实现、**不断连**；Report 上报后仍执行。
            // ★ 非法返回值按**失败关闭**处理：C# 的枚举允许任意整数，若写成
            //   「Reject / Report / 否则执行」，一个 (PMRpcValidation)99 就会变成**放行**。
            PMNet.PMRpcValidation pmVerdict = self.Push_ForceValidate(p0);
            if (pmVerdict != PMNet.PMRpcValidation.Accept)
            {
                PMNet.PMRpcValidation pmReported = pmVerdict == PMNet.PMRpcValidation.Report
                    ? PMNet.PMRpcValidation.Report
                    : PMNet.PMRpcValidation.Reject;
                PMNet.PMRpcValidationSink.NotifyReported(
                    t, PMGeneratedRpcId_Push, pmReported, "Push");

                if (pmReported == PMNet.PMRpcValidation.Reject)
                {
                    return;
                }
            }
            self.Push(p0);
        }

        /// <summary>
        /// 发送 helper（契约 §2 冻结格式 v1：**private**，只允许编织后的同类入口 Push 调用）。
        /// 先走 GetFunctionCallspace 判定，再按结果本地执行 / 发往远端。
        /// 业务请调用普通名 Push（编织后它是网络入口），不要直接调用本方法。
        /// </summary>
        private void PMNet_Push(float[] p0)
        {
            PMNet.PMFunctionCallspace callspace = PMNet.PMRpcDispatch.EvaluateCallspace(
                NetMode, Role, PMNet.PMRpcKind.Multicast, GetNetConnection() != null, false);

            float[] p0Snapshot = null;
            // 实参预处理（快照 / 长度门）必须在**本地执行之前**：
            // Multicast 在服务端会先本地执行、再外发，业务实现可以就地改写数组实参。
            if (PMNet.PMRpcDispatch.ShouldSendRemote(callspace))
            {
                if (p0 != null)
                {
                    if (p0.Length > PMGeneratedMaxArrayLength)
                    {
                        throw new System.FormatException("RPC Push 的数组实参 p0 超长（上限 4096）：" + p0.Length);
                    }

                    // 浅拷贝即足够：支持的元素集是整型/浮点/布尔/枚举/string，均为值类型或不可变引用。
                    p0Snapshot = (float[])p0.Clone();
                }

            }

            if (PMNet.PMRpcDispatch.ShouldExecuteLocal(callspace))
            {
                Push(p0);
            }

            if (PMNet.PMRpcDispatch.ShouldSendRemote(callspace))
            {
                // 闭包捕获实参；数组用快照（值类型/string 不必）：编码推迟到真正发送时也不会读到被覆盖的值。
                PMNet.Generated.PMNetGeneratedRegistry.EnqueueRemote(
                    this, PMGeneratedRpcId_Push,
                    delegate(PMNet.PMNetObject t, PMNet.PMNetWriter w)
                    {
                        float[] a = p0Snapshot;
                        if (a == null)
                        {
                            // -1 = null（契约 §6 的“长度 + 元素”需要一个 null 哨兵，否则 null 与空数组不可区分）
                            w.WriteSInt32(-1);
                        }
                        else
                        {
                            w.WriteSInt32(a.Length);
                            for (int i = 0; i < a.Length; i++)
                            {
                                w.WriteFloat(a[i]);
                            }
                        }
                    });
            }
        }

        /// <summary>RPC Fire 的稳定 ID（16 位，D-R0-49）。</summary>
        public const ushort PMGeneratedRpcId_Fire = 24024;

        /// <summary>读出参数、过校验、调用业务实现（接收侧分发）。</summary>
        private static void PMNet_RpcInvoke_Fire(PMNet.PMNetObject t, PMNet.PMNetReader r)
        {
            E2eReplicated self = (E2eReplicated)t;
            int p0 = r.ReadInt32();
            float p1 = r.ReadFloat();

            if (!r.IsAtEnd)
            {
                throw new System.FormatException("RPC Fire 载荷存在尾随字节：参数只占 " + r.Consumed + " 字节，载荷更长");
            }

            // 校验（ForceValidate 三态）：Reject 只跳过实现、**不断连**；Report 上报后仍执行。
            // ★ 非法返回值按**失败关闭**处理：C# 的枚举允许任意整数，若写成
            //   「Reject / Report / 否则执行」，一个 (PMRpcValidation)99 就会变成**放行**。
            PMNet.PMRpcValidation pmVerdict = self.Fire_ForceValidate(p0, p1);
            if (pmVerdict != PMNet.PMRpcValidation.Accept)
            {
                PMNet.PMRpcValidation pmReported = pmVerdict == PMNet.PMRpcValidation.Report
                    ? PMNet.PMRpcValidation.Report
                    : PMNet.PMRpcValidation.Reject;
                PMNet.PMRpcValidationSink.NotifyReported(
                    t, PMGeneratedRpcId_Fire, pmReported, "Fire");

                if (pmReported == PMNet.PMRpcValidation.Reject)
                {
                    return;
                }
            }
            self.Fire(p0, p1);
        }

        /// <summary>
        /// 发送 helper（契约 §2 冻结格式 v1：**private**，只允许编织后的同类入口 Fire 调用）。
        /// 先走 GetFunctionCallspace 判定，再按结果本地执行 / 发往远端。
        /// 业务请调用普通名 Fire（编织后它是网络入口），不要直接调用本方法。
        /// </summary>
        private void PMNet_Fire(int p0, float p1)
        {
            PMNet.PMFunctionCallspace callspace = PMNet.PMRpcDispatch.EvaluateCallspace(
                NetMode, Role, PMNet.PMRpcKind.Server, GetNetConnection() != null, false);

            // 实参预处理（快照 / 长度门）必须在**本地执行之前**：
            // Multicast 在服务端会先本地执行、再外发，业务实现可以就地改写数组实参。
            if (PMNet.PMRpcDispatch.ShouldSendRemote(callspace))
            {
            }

            if (PMNet.PMRpcDispatch.ShouldExecuteLocal(callspace))
            {
                Fire(p0, p1);
            }

            if (PMNet.PMRpcDispatch.ShouldSendRemote(callspace))
            {
                // 闭包捕获实参；数组用快照（值类型/string 不必）：编码推迟到真正发送时也不会读到被覆盖的值。
                PMNet.Generated.PMNetGeneratedRegistry.EnqueueRemote(
                    this, PMGeneratedRpcId_Fire,
                    delegate(PMNet.PMNetObject t, PMNet.PMNetWriter w)
                    {
                        w.WriteInt32(p0);
                        w.WriteFloat(p1);
                    });
            }
        }

        /// <summary>RPC Notify 的稳定 ID（16 位，D-R0-49）。</summary>
        public const ushort PMGeneratedRpcId_Notify = 44765;

        /// <summary>读出参数、过校验、调用业务实现（接收侧分发）。</summary>
        private static void PMNet_RpcInvoke_Notify(PMNet.PMNetObject t, PMNet.PMNetReader r)
        {
            E2eReplicated self = (E2eReplicated)t;
            int p0 = r.ReadInt32();

            if (!r.IsAtEnd)
            {
                throw new System.FormatException("RPC Notify 载荷存在尾随字节：参数只占 " + r.Consumed + " 字节，载荷更长");
            }
            self.Notify(p0);
        }

        /// <summary>
        /// 发送 helper（契约 §2 冻结格式 v1：**private**，只允许编织后的同类入口 Notify 调用）。
        /// 先走 GetFunctionCallspace 判定，再按结果本地执行 / 发往远端。
        /// 业务请调用普通名 Notify（编织后它是网络入口），不要直接调用本方法。
        /// </summary>
        private void PMNet_Notify(int p0)
        {
            PMNet.PMFunctionCallspace callspace = PMNet.PMRpcDispatch.EvaluateCallspace(
                NetMode, Role, PMNet.PMRpcKind.Client, GetNetConnection() != null, false);

            // 实参预处理（快照 / 长度门）必须在**本地执行之前**：
            // Multicast 在服务端会先本地执行、再外发，业务实现可以就地改写数组实参。
            if (PMNet.PMRpcDispatch.ShouldSendRemote(callspace))
            {
            }

            if (PMNet.PMRpcDispatch.ShouldExecuteLocal(callspace))
            {
                Notify(p0);
            }

            if (PMNet.PMRpcDispatch.ShouldSendRemote(callspace))
            {
                // 闭包捕获实参；数组用快照（值类型/string 不必）：编码推迟到真正发送时也不会读到被覆盖的值。
                PMNet.Generated.PMNetGeneratedRegistry.EnqueueRemote(
                    this, PMGeneratedRpcId_Notify,
                    delegate(PMNet.PMNetObject t, PMNet.PMNetWriter w)
                    {
                        w.WriteInt32(p0);
                    });
            }
        }

        /// <summary>RPC Warp 的稳定 ID（16 位，D-R0-49）。</summary>
        public const ushort PMGeneratedRpcId_Warp = 46137;

        /// <summary>读出参数、过校验、调用业务实现（接收侧分发）。</summary>
        private static void PMNet_RpcInvoke_Warp(PMNet.PMNetObject t, PMNet.PMNetReader r)
        {
            E2eReplicated self = (E2eReplicated)t;
            int p0 = r.ReadInt32();

            if (!r.IsAtEnd)
            {
                throw new System.FormatException("RPC Warp 载荷存在尾随字节：参数只占 " + r.Consumed + " 字节，载荷更长");
            }

            // 校验（ForceValidate 三态）：Reject 只跳过实现、**不断连**；Report 上报后仍执行。
            // ★ 非法返回值按**失败关闭**处理：C# 的枚举允许任意整数，若写成
            //   「Reject / Report / 否则执行」，一个 (PMRpcValidation)99 就会变成**放行**。
            PMNet.PMRpcValidation pmVerdict = self.Warp_ForceValidate(p0);
            if (pmVerdict != PMNet.PMRpcValidation.Accept)
            {
                PMNet.PMRpcValidation pmReported = pmVerdict == PMNet.PMRpcValidation.Report
                    ? PMNet.PMRpcValidation.Report
                    : PMNet.PMRpcValidation.Reject;
                PMNet.PMRpcValidationSink.NotifyReported(
                    t, PMGeneratedRpcId_Warp, pmReported, "Warp");

                if (pmReported == PMNet.PMRpcValidation.Reject)
                {
                    return;
                }
            }
            self.Warp(p0);
        }

        /// <summary>
        /// 发送 helper（契约 §2 冻结格式 v1：**private**，只允许编织后的同类入口 Warp 调用）。
        /// 先走 GetFunctionCallspace 判定，再按结果本地执行 / 发往远端。
        /// 业务请调用普通名 Warp（编织后它是网络入口），不要直接调用本方法。
        /// </summary>
        private void PMNet_Warp(int p0)
        {
            PMNet.PMFunctionCallspace callspace = PMNet.PMRpcDispatch.EvaluateCallspace(
                NetMode, Role, PMNet.PMRpcKind.Server, GetNetConnection() != null, false);

            // 实参预处理（快照 / 长度门）必须在**本地执行之前**：
            // Multicast 在服务端会先本地执行、再外发，业务实现可以就地改写数组实参。
            if (PMNet.PMRpcDispatch.ShouldSendRemote(callspace))
            {
            }

            if (PMNet.PMRpcDispatch.ShouldExecuteLocal(callspace))
            {
                Warp(p0);
            }

            if (PMNet.PMRpcDispatch.ShouldSendRemote(callspace))
            {
                // 闭包捕获实参；数组用快照（值类型/string 不必）：编码推迟到真正发送时也不会读到被覆盖的值。
                PMNet.Generated.PMNetGeneratedRegistry.EnqueueRemote(
                    this, PMGeneratedRpcId_Warp,
                    delegate(PMNet.PMNetObject t, PMNet.PMNetWriter w)
                    {
                        w.WriteInt32(p0);
                    });
            }
        }

        // ---------------- 静态注册表条目 ----------------

        /// <summary>
        /// 本类贡献给 PMNetRegistry 的条目。
        /// 协议摘要用 PMStableHash 现算（与生成期同一实现），不写死字面量。
        /// 第一条语句是编织门：未编织程序集在这里就被拒。
        /// RegisterAll 会先把所有类的 BuildEntry 求值完再 RegisterClass，
        /// 因此这里抛出时全局注册表还是空的（不会留下半注册）。
        /// </summary>
        internal static PMNet.PMNetClassEntry PMNet_BuildEntry()
        {
            PMNet_RequireRpcWeave();

            PMNet.PMPropertyDescriptor[] props = new PMNet.PMPropertyDescriptor[4];
            PMNet.PMPropertyDescriptor p0 = new PMNet.PMPropertyDescriptor();
            p0.PropertyId = 15488;
            p0.Condition = PMNet.PMCond.None;
            p0.MaskOffset = 0;
            p0.MaskBitCount = 1;
            p0.QuantizerId = 0;
            p0.OnRepMethodId = 0;
            p0.PushBased = true;
            p0.MemberName = "_title";
            p0.SetterName = "PMNet_Set_title";
            p0.Writer = PMNet_Write__title;
            p0.Reader = PMNet_Read__title;
            props[0] = p0;
            PMNet.PMPropertyDescriptor p1 = new PMNet.PMPropertyDescriptor();
            p1.PropertyId = 26362;
            p1.Condition = PMNet.PMCond.None;
            p1.MaskOffset = 1;
            p1.MaskBitCount = 1;
            p1.QuantizerId = 0;
            p1.OnRepMethodId = 0;
            p1.PushBased = true;
            p1.MemberName = "_speed";
            p1.SetterName = "PMNet_Set_speed";
            p1.Writer = PMNet_Write__speed;
            p1.Reader = PMNet_Read__speed;
            props[1] = p1;
            PMNet.PMPropertyDescriptor p2 = new PMNet.PMPropertyDescriptor();
            p2.PropertyId = 36071;
            p2.Condition = PMNet.PMCond.OwnerOnly;
            p2.MaskOffset = 2;
            p2.MaskBitCount = 1;
            p2.QuantizerId = 0;
            p2.OnRepMethodId = 0;
            p2.PushBased = true;
            p2.MemberName = "_ammo";
            p2.SetterName = "PMNet_Set_ammo";
            p2.Writer = PMNet_Write__ammo;
            p2.Reader = PMNet_Read__ammo;
            props[2] = p2;
            PMNet.PMPropertyDescriptor p3 = new PMNet.PMPropertyDescriptor();
            p3.PropertyId = 43547;
            p3.Condition = PMNet.PMCond.None;
            p3.MaskOffset = 3;
            p3.MaskBitCount = 1;
            p3.QuantizerId = 0;
            p3.OnRepMethodId = 1;
            p3.PushBased = true;
            p3.MemberName = "_health";
            p3.SetterName = "PMNet_Set_health";
            p3.Writer = PMNet_Write__health;
            p3.Reader = PMNet_Read__health;
            props[3] = p3;

            PMNet.PMReplicationDescriptor rep = new PMNet.PMReplicationDescriptor();
            rep.ClassId = PMGeneratedClassId;
            rep.Properties = props;
            rep.ChangeMaskBitCount = PMGeneratedChangeMaskBitCount;
            rep.HasConditionalMask = true;
            rep.ProtocolHash = PMNet.PMStableHash.ClassProtocolHash(PMGeneratedClassId, props);
            rep.TypeName = "PMNetE2E.E2eReplicated";

            PMNet.PMNetRpcEntry[] rpcs = new PMNet.PMNetRpcEntry[6];
            PMNet.PMRpcDescriptor d0 = new PMNet.PMRpcDescriptor();
            d0.RpcId = PMGeneratedRpcId_Say;
            d0.Direction = PMNet.PMRpcKind.Client;
            d0.IsReliable = true;
            d0.Validator = PMNet.PMRpcValidator.None;
            d0.ParamLayoutId = 9527;
            d0.MethodName = "Say";

            PMNet.PMNetRpcEntry r0 = new PMNet.PMNetRpcEntry();
            r0.Descriptor = d0;
            r0.OwningClassId = PMGeneratedClassId;
            r0.Invoke = PMNet_RpcInvoke_Say;
            rpcs[0] = r0;
            PMNet.PMRpcDescriptor d1 = new PMNet.PMRpcDescriptor();
            d1.RpcId = PMGeneratedRpcId_Checked;
            d1.Direction = PMNet.PMRpcKind.Server;
            d1.IsReliable = true;
            d1.Validator = PMNet.PMRpcValidator.Validate;
            d1.ParamLayoutId = 21580;
            d1.MethodName = "Checked";

            PMNet.PMNetRpcEntry r1 = new PMNet.PMNetRpcEntry();
            r1.Descriptor = d1;
            r1.OwningClassId = PMGeneratedClassId;
            r1.Invoke = PMNet_RpcInvoke_Checked;
            rpcs[1] = r1;
            PMNet.PMRpcDescriptor d2 = new PMNet.PMRpcDescriptor();
            d2.RpcId = PMGeneratedRpcId_Push;
            d2.Direction = PMNet.PMRpcKind.Multicast;
            d2.IsReliable = true;
            d2.Validator = PMNet.PMRpcValidator.ForceValidate;
            d2.ParamLayoutId = 52223;
            d2.MethodName = "Push";

            PMNet.PMNetRpcEntry r2 = new PMNet.PMNetRpcEntry();
            r2.Descriptor = d2;
            r2.OwningClassId = PMGeneratedClassId;
            r2.Invoke = PMNet_RpcInvoke_Push;
            rpcs[2] = r2;
            PMNet.PMRpcDescriptor d3 = new PMNet.PMRpcDescriptor();
            d3.RpcId = PMGeneratedRpcId_Fire;
            d3.Direction = PMNet.PMRpcKind.Server;
            d3.IsReliable = true;
            d3.Validator = PMNet.PMRpcValidator.ForceValidate;
            d3.ParamLayoutId = 26502;
            d3.MethodName = "Fire";

            PMNet.PMNetRpcEntry r3 = new PMNet.PMNetRpcEntry();
            r3.Descriptor = d3;
            r3.OwningClassId = PMGeneratedClassId;
            r3.Invoke = PMNet_RpcInvoke_Fire;
            rpcs[3] = r3;
            PMNet.PMRpcDescriptor d4 = new PMNet.PMRpcDescriptor();
            d4.RpcId = PMGeneratedRpcId_Notify;
            d4.Direction = PMNet.PMRpcKind.Client;
            d4.IsReliable = true;
            d4.Validator = PMNet.PMRpcValidator.None;
            d4.ParamLayoutId = 21580;
            d4.MethodName = "Notify";

            PMNet.PMNetRpcEntry r4 = new PMNet.PMNetRpcEntry();
            r4.Descriptor = d4;
            r4.OwningClassId = PMGeneratedClassId;
            r4.Invoke = PMNet_RpcInvoke_Notify;
            rpcs[4] = r4;
            PMNet.PMRpcDescriptor d5 = new PMNet.PMRpcDescriptor();
            d5.RpcId = PMGeneratedRpcId_Warp;
            d5.Direction = PMNet.PMRpcKind.Server;
            d5.IsReliable = true;
            d5.Validator = PMNet.PMRpcValidator.ForceValidate;
            d5.ParamLayoutId = 21580;
            d5.MethodName = "Warp";

            PMNet.PMNetRpcEntry r5 = new PMNet.PMNetRpcEntry();
            r5.Descriptor = d5;
            r5.OwningClassId = PMGeneratedClassId;
            r5.Invoke = PMNet_RpcInvoke_Warp;
            rpcs[5] = r5;

            PMNet.PMNetClassEntry entry = new PMNet.PMNetClassEntry();
            entry.ClassId = PMGeneratedClassId;
            entry.TypeName = "PMNetE2E.E2eReplicated";
            entry.Rep = rep;
            entry.Rpcs = rpcs;
            entry.Factory = PMNet_CreateInstance;
            return entry;
        }

        /// <summary>构造工厂（对应 PMNetWorld.RegisterClass 的工厂参数）。</summary>
        private static PMNet.PMNetObject PMNet_CreateInstance()
        {
            return new global::PMNetE2E.E2eReplicated();
        }
    }
}
