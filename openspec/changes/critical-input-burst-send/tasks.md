## 1. 客户端关键输入判定与同帧重复发送

- [x] 1.1 在客户端战斗 tick 中判定关键输入帧，区分停步边沿与本帧新攻击
- [x] 1.2 在 UDP 发送入口支持同一逻辑帧重复发送同一份 `BattleInfo` 2~3 次
- [x] 1.3 确保 burst-send 重复包复用相同 `OperationID`、`ClientAckedFrame`、移动值与 `AttackId` 载荷

## 2. 服务端幂等接收与验证日志

- [x] 2.1 校验并保持同一 `SyncFrameId` 重复移动输入的单条缓冲覆盖语义
- [x] 2.2 校验并保持同一 `AttackId` 重复攻击输入只处理一次的去重语义
- [x] 2.3 为关键输入 burst-send 增加客户端与服务端验证日志，便于联调确认触发与去重行为

## 3. 文档与联调验证

- [x] 3.1 同步更新客户端/服务端协作文档与 BothSide 说明关键输入 burst-send 语义
- [ ] 3.2 在丢包环境下执行停步与攻击联调，确认停步前弹频率下降且攻击未重复结算
